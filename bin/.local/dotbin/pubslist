#!/bin/bash

n_papers=$(pubs statistics | awk '/Total papers/ {print $4}')

# list publications
# show rofi selection
# open using pubs
pubs list \
    | rofi -dmenu -i -columns 1 -lines ${n_papers} -p "Paper:" -location 0 -width 80%\
    | sed -e 's/\[//' -e s'/\].*//' \
    | xargs pubs doc open
