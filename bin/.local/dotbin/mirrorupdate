#!/bin/bash
curl -fsSL \
    "https://archlinux.org/mirrorlist/?country=DE&protocol=https&use_mirror_status=on" |
    sed -e 's/^#Server/Server/' -e '/^#/d' |
    rankmirrors -p -v -n 5 - |
    tee "${HOME}/.config/yay/mirrorlist"
