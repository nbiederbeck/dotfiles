# vim: commentstring=#%s
super + Return
    kitty

super + XF86MonBrightnessUp
    qdbus org.kde.Solid.PowerManagement /org/kde/Solid/PowerManagement/Actions/BrightnessControl setBrightness 851

super + XF86MonBrightnessDown
    qdbus org.kde.Solid.PowerManagement /org/kde/Solid/PowerManagement/Actions/BrightnessControl setBrightness 1

# super + d
#     rofi -show run

# super + shift + d
#     rofi -show window -lines 5

# super + @space
#     bspc node --state \~floating

# super + e
#     kde-bspwm-lock

# super + shift + r
#     bspc wm --restart & notify-send "Restart" "Window manager restarted."

# super + shift + c
#     sxhkd --restart

# super + shift + q
#     bspc node --kill

# # 'monocle' mode a.k.a. fullscreen
# super + m
#     bspc desktop --layout next

# super + {h,j,k,l}
#     bspc node --focus {west,south,north,east}

# super + shift + {h,j,k,l}
#     bspc node -s {west,south,north,east}

# # focus or send to the given desktop
# super + {_,shift + }{1-9,0}
#     bspc {desktop -f,node -d} '^{1-9,10}'

# # focus the last node/desktop
# super + {grave,Tab}
#     bspc {node,desktop} --focus last

# super + {Left,Right}
#     bspc desktop --focus {prev,next}

# super + {t,shift + t,s}
#     bspc node --state {tiled,pseudo_tiled,floating}

# super + f
#     bspc node --state \~fullscreen

super + shift + p
    passmenu
